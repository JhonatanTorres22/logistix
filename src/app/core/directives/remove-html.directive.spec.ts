import { RemoveHtmlDirective } from './remove-html.directive';

describe('RemoveHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
