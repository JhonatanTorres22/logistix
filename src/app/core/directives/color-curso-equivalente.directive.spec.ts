import { ColorCursoEquivalenteDirective } from './color-curso-equivalente.directive';

describe('ColorCursoEquivalenteDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCursoEquivalenteDirective();
    expect(directive).toBeTruthy();
  });
});
