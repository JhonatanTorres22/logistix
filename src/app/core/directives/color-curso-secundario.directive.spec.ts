import { ColorCursoSecundarioDirective } from './color-curso-secundario.directive';

describe('ColorCursoSecundarioDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCursoSecundarioDirective();
    expect(directive).toBeTruthy();
  });
});
